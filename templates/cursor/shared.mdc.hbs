---
description: Shared project context for {{repoName}}
globs: []
alwaysApply: true
---

# {{repoName}} — Project Context

**GitHub:** `{{repo}}`{{#if project}}
**Project Board:** {{project}}{{/if}}

## Project Areas

{{#each areas}}
- **{{name}}** (`{{path}}/`) — {{stack}}
{{/each}}

## GitHub Issue Labels

**Type:** {{#each labels.types}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}

**Area:** {{#each labels.areas}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}

**Priority:** {{#each labels.priorities}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}

## Creating Issues

Run `npx issueforge issue` in the terminal, or ask the AI to create an issue using the labels above targeting repo `{{repo}}`.

Auth: `gh auth login` (no token files needed).
